{
  "name": "pub",
  "version": "1.0.0",
  "description": "Tool for simplifying creation process of dockerfiles",
  "repository": {
    "type": "git",
    "url": "https://github.com/v2p/pub.git"
  },
  "license": "MIT",
  "main": "./main.js",
  "scripts": {
    "watch": "$(npm bin)/babel ./src --watch --out-file ./build/app.js",
    "prestart": "$(npm bin)/babel ./src --out-file ./build/app.js",
    "start": "node ./build/app.js"
  },
  "engines": {
    "node": ">=6.7"
  },
  "devDependencies": {
    "eslint": "^3.10.1",
    "babel-cli": "^6.18.0",
    "babel-preset-es2015": "^6.18.0"
  }
}
